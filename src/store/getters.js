const getters = {
    clientWidth: state => state.home.clientWidth,
    headShow: state => state.home.headShow,
    footShow: state => state.home.footShow,
    headIndex: state => state.home.headIndex
  }
  export default getters
  